# 개발 로그

주간 회고. 무엇을 배웠고, 어디서 막혔고, 왜 그렇게 설계했는지.
6~7개월 뒤 발표 자료의 원천이 된다.

---

## 2026-09-10 — Phase 0: 스캐폴드와 하네스

### 한 것

프로젝트 뼈대, 품질 게이트, ADR 2건. 로직 코드는 없다.

### 배운 것

**명세가 실물과 다른 지점이 셋 있었다.**

1. "공식 HTML tarball" 을 수집원으로 정했는데, `postgresql.org/docs/` 가 링크하는
   다운로드는 **PDF 뿐**이었다. HTML tarball 은 FTP 릴리스 디렉터리
   (`ftp.postgresql.org/pub/source/v18.6/postgresql-18.6-docs.tar.gz`)에만 있다.
   결정 자체는 유효했지만 경로가 달랐다.

2. 문서 구조를 WebFetch 로 조사했더니 "에러 코드는 HTML table 이 아니라 definition
   list" 라는 답이 왔다. **틀렸다.** 원본을 직접 받아보니 `<table class="table">` 이다.
   WebFetch 는 HTML 을 마크다운으로 변환한 뒤 답하므로 원본 구조 질문에는 쓸 수 없다.
   → 문서 구조 판단은 `curl` + 원본으로만 한다.

3. GitHub Actions 버전을 기억으로 적었다면 전부 틀렸을 것이다.
   `setup-uv` 는 v10, `checkout` 은 v7 이었다(v5/v4 로 적을 뻔했다).
   그런데 조회만으로도 부족했다. `releases/latest` 가 알려준 `v10.0.1` 을 보고
   `@v10` 으로 적었는데 CI 가 "unable to find version v10" 으로 죽었다.
   setup-uv 는 major alias 태그를 v7 까지만 유지하고 v8~v10 에는 만들지 않는다.
   → 릴리스 태그 이름과 실제로 참조 가능한 ref 는 다르다. 쓰려는 ref 가
     존재하는지를 `git ls-remote --tags` 로 따로 확인한다.

**변이 검증이 실제로 죽은 코드를 잡았다.**

'테스트 0개는 실패' 가드에 `-k` / `-m` 필터 예외 분기를 넣어두었다. 그런데 그 분기를
통째로 지워도 **어떤 테스트도 실패하지 않았다.** 조사해보니 이 훅이 pytest 내장
deselection 보다 **먼저** 호출되어 항상 필터 전 items 를 보고 있었다. 분기는 도달할 수
없는 코드였고, 더 나쁘게는 가드가 최종 수집 결과가 아니라 필터 전 상태를 검사하고 있었다.

`@pytest.hookimpl(trylast=True)` 로 순서를 고정하니 분기가 살아났고, "다른 플러그인이
전부 deselect 한 경우"까지 잡게 되었다. 변이 검증을 안 했으면 **가드가 있다고 믿으면서
실제로는 절반만 작동하는 상태**로 갔을 것이다.

→ BRIEF §6.4 의 "깨뜨려본 적 없는 가드는 없는 가드다" 가 문자 그대로 맞았다.

**pre-commit 도 같은 함정이 있었다.**

첫 실행에서 모든 훅이 `(no files to check) Skipped` 인데 exit 0 이었다. git 이 추적하는
파일이 없었기 때문이다. skip 은 통과처럼 보이는 미실행이라는 것을 도구 자체가 보여줬다.

### 막힌 것

없음. 다만 프로토콜(`Source`)이 아직 검증되지 않은 상태라는 점을 인지하고 있다.
구현체가 하나뿐이라 인터페이스가 옳은지 알 방법이 없다. 두 번째 소스에서 고칠 것을
전제로 두고, 그 위에 여러 계층을 쌓지 않는다.

### 검증

ruff clean / mypy strict 12 files / pytest 16 passed, **0 skipped**.
가드 2건 각각 양방향 변이 검증 수행 후 복원.

### 다음

Phase 1 — 문서 수집·파싱·청킹. 시작 전 선행 확인부터.

---

## 2026-09-10 — Phase 1: 수집·파싱·청킹

### 한 것

tarball 수집, DocBook HTML 파싱, 구조 보존 청킹, CLI. 문서 1,131개에서 청크
16,442개가 1.7초에 나온다. LLM 은 아직 없다.

### 배운 것

**선행 확인이 설계를 바꿨다.** 코드를 쓰기 전에 문서 1,148개를 전부 훑었고, 그
결과가 사전 가정을 뒤집었다. "설정 표"가 주요 난제라고 보았는데 설정 파라미터는
표가 아니라 `<dl class="variablelist">` 였다. `<dt>` 12,992개 대 표 486개.
게다가 정의 항목은 median 179자로 대부분 청크 하나에 그대로 들어간다.
실측하지 않았다면 있지도 않은 문제를 풀고 있었을 것이다.

**같은 이름의 수치라도 무엇을 재는지가 다르면 다른 값이다.**
`TABLE_MAX_BYTES` 를 8,192 로 정하며 "표의 상위 15%가 걸린다"고 ADR 에 적었는데,
그 15%는 **원본 HTML 바이트** 기준이었다. 실제 적용 대상은 태그가 빠진 직렬화
결과이고 거기서는 2.7%만 걸렸다. 거의 걸리지 않는 임계값은 검증된 적 없는 코드다.
4,096 으로 낮춰 9.3%가 걸리게 하고 ADR 을 정정했다.

**규칙보다 모델이 낫다.** 청킹 코드를 처음 쓸 때 `chunk.py` 가
`postgres_docs.parse` 를 직접 import 하고 `source_id="postgres_docs"` 를
하드코딩했다. 계층 가드는 `pipeline -> sources` 를 허용하므로 이것을 잡지 못한다.
`Document.source_id` 와 `ref_for()` 를 도메인에 넣으니 위반할 방법 자체가
사라졌다. 지킬 수 없는 규칙보다 어길 수 없는 구조가 낫다.

**변이 검증이 또 공허한 테스트를 잡았다.** "코드는 절대 자르지 않는다" 테스트가
코드 보호를 제거해도 통과했다. 테스트 본문에 마침표가 없어 산문 분할기가 문장
경계를 못 찾았고, 그래서 보호 장치 없이도 안 잘렸다. 본문에 마침표를 넣으니 변이가
정확히 잡힌다. 두 Phase 연속으로 변이 검증이 실제 결함을 잡았다.

**고치고 싶은 것을 안 고쳤다.** 50자 미만 청크가 13.95%(2,294개) 나온다.
`or` 두 글자짜리 청크가 검색에 좋을 리 없다. 그런데 평가셋이 없는 상태에서
고치면 개선인지 노이즈인지 알 방법이 없다. 측정값으로 기록만 하고 Phase 3
이후로 미뤘다. 이것이 Phase 3 을 앞에 둔 이유를 실감한 순간이었다.

### 막힌 것

없음. `Source` 프로토콜은 여전히 검증되지 않았다 — 구현체가 하나뿐이다.
Phase 1 을 지나며 이 인터페이스가 `fetch` / `parse` 두 단계로 충분한지 감이
생겼지만, 두 번째 소스를 넣기 전에는 확인할 방법이 없다.

### 검증

ruff clean / mypy strict 24 files / pytest 81 passed, **0 skipped**.
청킹 가드 3건 양방향 변이 검증 후 복원.

### 다음

Phase 2 — 첫 LLM 호출, 구조화 출력, 재시도.

---

## 2026-09-10 — Phase 2: 첫 LLM 호출과 구조화 출력

### 한 것

Gemini Flash 로 에러 메시지에 구조화된 진단을 낸다. 공급사·모델은 설정으로 분리했고,
근거는 `{chunk_id, quote}` 로 받아 **둘 다 검증한다.** 검색은 없다 — 청크는 직접 지정한다.

### 배운 것

**선행 확인이 또 명세를 고쳤다.** 모델 목록에 있다고 쓸 수 있는 것이 아니었다.
`gemini-2.5-flash` 는 404 와 함께 "신규 사용자에게 제공되지 않는다"를 돌려줬고,
`gemini-3.8-flash` 는 다섯 번 중 세 번 503 이었다. 문서를 읽고 고른 게 아니라 전부
호출해보고 골랐다.

**SDK 가 이미 하는 일을 먼저 확인했다.** 재시도를 만들기 전에 google-genai 소스를
열어봤더니 5회 / 지수 백오프 / 대상 코드에 **429 포함**이었다. 무료 티어 분당 제한은
SDK 가 처리한다는 뜻이다. 그 위에 얹은 것은 모델 폴백 하나뿐이고, 그것도 실제로
필요했다 — `gemini-3.7-flash` 가 재시도 5회를 다 쓰고 죽은 뒤 `3.5-flash` 로 넘어가
정상 응답을 받았다.

**검증이 첫 호출부터 값을 했다.** 모델이 `'[runtime-config-connection#...:2]'` 처럼
대괄호를 포함해 인용했다. 환각은 아니고 형식 오염이지만, 검증이 없었다면 그대로
통과했을 것이다. 원인은 우리 컨텍스트 표기였다 — `[chunk_id]` 로 보여주니 대괄호까지
복사한 것이다. **표기를 `<chunk id="...">` 로 바꿔 원인을 없애고, 정규화는 안전망으로만
남겼다.** 정규화만 했다면 원인은 그대로 둔 채 증상만 가렸을 것이다.

**우리 쪽 결함도 검증이 잡았다.** 모델이 `"max_connections (integer)\nDetermines the…"`
를 인용했는데 탈락했다. 컨텍스트에는 heading 과 text 를 이어 보여주면서 검증은 text 에만
했기 때문이다. 모델은 정직했고 우리가 틀렸다. `Chunk.quotable` 하나로 묶어 렌더링과
검증이 같은 문자열을 보게 했다.

**규칙보다 구조.** 청킹 때 배운 것이 반복됐다. "근거 없는 주장을 내지 않는다"를 지시로만
두면 지켜지지 않는다. 근거를 하나도 살리지 못한 원인 후보를 **검증 단계에서 제거**하니
규칙을 어길 방법이 없어졌다.

**프롬프트가 의도대로 읽히지 않았다.** 정답 청크를 주고도 빈 답이 나와 원문을 봤더니
모델의 추론이 이랬다 — *"문서에 'sorry, too many clients already' 라는 문자열이 없으니
근거가 없다."* 문서는 에러 문자열을 그대로 싣지 않는다. 우리 지시가 "인용은 주장을
뒷받침하면 된다"가 아니라 "에러가 문서에 있어야 한다"로 읽힌 것이다. 한 문장 추가로
고쳤다. **이건 프롬프트 튜닝이 아니라 오작동 수정이다** — 정답을 주고도 답을 못 내면
"동작하는 최소한"에 도달한 게 아니다.

**실패를 한 덩어리로 보면 고칠 수 없다.** 위 문제를 처음엔 짚지 못했다. "근거 부족"
하나로만 보여서, 모델이 거부한 것인지 우리 검증이 다 걸러낸 것인지 알 수 없었기
때문이다. `insufficient_cause` 로 셋(`model_declined` / `verification_removed_all` /
`no_causes_returned`)을 나누니 곧바로 드러났다. **관측 부족은 그 자체가 결함이다.**

**저장 메타가 본문의 89% 다.** 청크의 참조 메타(`ref`)가 본문 대비 89% 이고, 그래서
JSONL 이 14MB 다.

> **정정 (Phase 3 선행 확인).** 이것을 "컨텍스트 비용"으로 적었는데 **틀렸다.**
> 실제 프롬프트에는 `<chunk id="...">본문</chunk>` 만 들어가고 `ref` 전체는 JSONL 에만
> 있다. 89% 는 프롬프트 비용이 아니라 저장 비용이며 Phase 7 과 무관하다. `ref` 를
> 프롬프트에 넣는다면을 가정한 계산이었는데, 실제 설계는 chunk_id 만 노출하는 쪽으로
> 결정됐다. **잘못된 전제가 회고에 남으면 나중에 그것을 근거로 잘못 판단한다.**

### 막힌 것

없음. 다만 모델 선택이 측정에 근거하지 않는다는 점이 계속 걸린다. 지연과 가용성만 보고
골랐고, 품질은 Phase 3 전에는 알 수 없다. ADR 0004 에 "잠정"으로 명시했다.

### 검증

ruff clean / mypy strict 34 files / pytest 124 passed, **0 skipped**.
가드 6건 양방향 변이 검증 후 복원 — 키 마스킹, 예외 체인 차단, quote 검증,
근거 0개 원인 제거, insufficient 판정, 책임 소재 구분.

### 다음

Phase 3 — 평가셋 구축. 여기서 모델·프롬프트·임계값 선택이 처음으로 측정에 근거하게 된다.

---

## 2026-09-11 — Phase 3: 평가셋과 기준선 (진행 중)

### 한 것

30문항 평가셋, 기계 채점기 7항목, judge, 평가 실행기, 라벨링 도구. 개선용 20문항
기준선을 잡았다. 사람 채점과 judge 일치율 측정이 남았다.

### 배운 것

**평가셋을 만드는 사람이 자기 평가셋을 의심해야 한다.** 이번에 내가 만든 문항에서
문제를 다섯 개 찾았다.

- `pgbouncer`, `mysql` 을 `no_answer` 후보로 넣으려다 **코퍼스에 실제로 있어서** 뺐다.
  안 잡았으면 그 문항들이 정반대를 검증했을 것이다 — 시스템이 정답을 찾아내면 오히려
  실패로 채점된다
- `e07` 을 "문서가 직접 다루지 않는 경계 케이스" 로 넣었는데 **실측해보니 근거가
  명확히 있었다.** 반대 방향으로 틀린 경우다
- `e05` 의 정답으로 `work_mem` 을 붙였는데 코퍼스에 `resize shared memory` 0건,
  `/dev/shm` 0건이고 `work_mem` 본문에 병렬·DSM 언급이 없었다. **확신 없는 라벨은
  평가셋 전체를 오염시키므로** 문항을 교체했다
- `c04`("컨테이너에서 메모리 설정")와 `n03`("Docker 에서 자동 조정", no_answer)의
  경계가 겹쳤다. 메모에 "문서에 컨테이너 언급이 없다" 고 써놓고 정답 청크를 지정한
  모순도 있었다. 질문에서 컨테이너를 뺐다
- `c06` 이 "버전 한계를 밝히는지" 검증하려 했는데 **그것을 재는 항목이 없었다.**
  `version_matches` 는 컨텍스트 청크의 버전만 보므로 항상 통과한다.
  문항은 있는데 채점 항목이 없으면 그 문항은 아무것도 검증하지 않는다

**`gold_cited` 가 "하나라도 인용" 이면 통과했다.** multi_chunk 문항 8개가 "여러 청크가
필요하다" 를 검증하려는데 하나만 맞아도 통과하고 있었다. `all_gold_cited` 를 따로
두니 5/7 로 드러났다. 같은 종류의 결함이 두 군데 있었던 셈이다.

**temperature 를 지정하지 않으면 평가가 성립하지 않는다.** 같은 20문항을 같은 모델·
프롬프트로 두 번 돌렸더니 통과율이 90% 와 75% 로 갈렸다. 원인을 찾는 데 시간을 썼는데,
공급사 기본 온도가 비결정적이라서였다. 0 으로 고정하니 **101개 항목이 전부 재현**된다.
"무엇이 개선인지 알 수 없는 상태" 가 이런 모습이다.

**캐시가 채점 결과까지 담고 있었다.** 채점기를 고치면 낡은 판정이 재사용된다.
채점은 공짜이므로 캐시할 이유가 없는데, 처음에 `CaseResult` 를 통째로 저장했다.
캐시에는 LLM 응답만 담고 채점은 매번 다시 하도록 고쳤다.

**429 를 처음 만났다.** Phase 2 에서는 한 번도 보지 못한 것이다. SDK 재시도 5회와 우리
재실행 3회가 **모두 같은 레이트 리밋 윈도우 안에서 소진**됐다 — 백오프 총합이 15초인데
윈도우는 60초다. 백오프를 늘리는 대신 호출 간격을 4초로 두어 애초에 한도를 넘지 않게
했다. 명세가 "부족하면 그때 만든다" 고 한 것이 정확했다. 미리 만들었다면 `retry-after`
존중 같은 걸 넣었을 텐데, 그것으로는 이 문제가 풀리지 않는다.

**bytecode 캐시에 한 번 속았다.** `inspect.getsource` 는 새 코드를 보여주는데 실행은
낡은 `.pyc` 를 쓰고 있었다. 코드가 분명히 있는데 동작하지 않아 한참 뒤졌다.

### 기준선 (improve 20문항, gemini-3.1-flash-lite, temperature 0)

```
완전성 True   전 항목 통과 75.0%
schema_valid 20/20 · version_matches 18/18 · no_answer_declined 2/2
cited_ids_exist 15/18 · quotes_verified 15/18 · gold_cited 15/18
all_gold_cited 5/7
```

실패는 세 갈래다 — (A) `chunk_id` 축약, (B) 긴 인용의 변형, (C) 정답 일부 누락.
§10 에서 이 셋을 따로 세야 무엇이 무엇을 고쳤는지 보인다.

### 막힌 것

없음. 다만 문항 30개가 적다는 것이 계속 걸린다. 한 문항이 5% 이므로 작은 차이를
유의미하다고 읽지 않도록 주의해야 한다.

### 다음

사람 채점 → judge 일치율 → 임계값 결정 → §10 잠정 항목 비교 → holdout 확인.
