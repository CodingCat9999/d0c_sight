# CLAUDE.md

> 매 세션 처음 읽는 규칙 파일. 작업 전에 「현재 상태」와 「미결 항목」을 먼저 확인한다.

## 프로젝트

인프라 에러 메시지를 입력하면 원인 후보와 대응 절차를 **근거와 함께** 제시하는 시스템.
1차 도메인은 PostgreSQL 18. 포트폴리오 프로젝트이며 면접 발표
(동기 → 문제 → 방법 → 결과 → 향후 개선)를 전제로 설계한다.

---

## 절대 규칙

1. **근거 없는 답변은 내지 않는다.**
   모든 주장은 문서와 절 단위로 추적 가능해야 한다. 틀렸을 때 원인이 데이터인지
   검색인지 모델인지 구별할 수 있어야 한다.

2. **결정론 계층과 LLM 계층을 섞지 않는다.**
   파싱·청킹·인덱싱은 결정론적 코드다. LLM 은 계산하지 않고 이미 구조화된 것을
   해석한다. 이 경계가 흔들리면 이후 품질 측정이 무의미해진다.

3. **N=1 에서 일반화하지 않는다.**
   인터페이스는 지금 잡되 구현체는 하나. 두 번째 사례가 생겼을 때 인터페이스를
   고친다. 상상으로 만든 추상화는 실제 두 번째 사례를 만나면 어차피 틀린다.

4. **한 커밋 = 한 논리 변경. 모든 커밋이 독립적으로 green.**
   커밋 메시지 본문에 "왜"를 쓴다. "무엇"은 diff 가 말한다. 두 관심사를 물리적으로
   분리할 수 없으면 그 사실을 커밋 메시지에 적는다.

5. **모든 가드에 짝 테스트를 붙인다.**
   가드를 만들었으면 의도적으로 무력화한 버전에서 테스트가 실패하는지, 그리고
   항상 발동시킨 버전에서 다른 테스트가 실패하는지 **양방향으로 확인하고 되돌린다.**
   깨뜨려본 적 없는 가드는 없는 가드다.

6. **부재 단언 앞에 존재 단언.**
   `not in` 류는 아무것도 생성되지 않아도 통과한다. "무언가 실제로 만들어졌다"를
   먼저 단언한다.

7. **skip 은 통과가 아니라 미실행이다.**
   상태 보고에 skip 수를 명시한다. 테스트 0개는 실패로 처리한다.

8. **측정 한계를 결과에 실어 보낸다.**
   검색 결과가 부족했으면, 근거 신뢰도가 낮으면, 버전이 불확실하면 그 사실을 함께
   낸다. LLM 프롬프트에도 한계 정보를 주입한다. 그러지 않으면 데이터는 정직한데
   해석이 거짓말을 한다.

9. **명세와 실물이 다르면 실물을 따르고 반드시 보고한다.**
   명세에 없는 케이스를 추측으로 채우지 않는다. 설계 판단이 갈리면 선택지와
   트레이드오프를 제시하고 멈춘다.

10. **수치 임계값에는 한 문장으로 말할 수 있는 뜻이 있어야 한다.**
    실측에서 전부 걸리면 잡음이고, 하나도 안 걸리면 검증된 적 없는 코드다.
    임계값을 결과에 함께 실어 나중에 과거 결과와 비교할 수 있게 한다.

---

## 명령어

```bash
uv sync                    # 의존성 설치
uv run pytest              # 테스트 (0개 수집은 실패)
uv run ruff check .        # 린트
uv run ruff format .       # 포맷
uv run mypy                # 타입 검사 (strict)
uv run pre-commit run --all-files   # 전체 훅

uv run d0c-sight ingest            # 문서 수집 → 청크 JSONL
uv run d0c-sight diagnose "에러" --chunk-id ID   # 진단 (검색 없음, 청크 직접 지정)
uv run python scripts/live_check.py             # 실제 API 호출 확인 (CI 에서 안 돈다)
```

**API 호출 테스트는 CI 에서 돌지 않는다.** 단위 테스트는 공급사를 더블로 고정하고,
실제 호출은 `scripts/live_check.py` 에만 있다. 무료 티어의 503 으로 CI 가 빨개지거나
매 커밋마다 쿼터를 쓰는 일을 막는다.

**무료 티어는 입출력이 모델 개선에 쓰일 수 있다. 공개 문서만 넣는다.**

**주의**: `pre-commit run` 은 git 이 추적하는 파일만 본다. staged 가 없으면 전부
`Skipped` 인데 exit 0 이다. 검증할 때는 `--all-files` 와 함께 파일이 실제로
검사되었는지 출력을 확인한다.

---

## 아키텍처 경계

```
domain  ←  sources  ←  pipeline  ←  cli
   ↖──────  llm  ───────────────────↗
```

`llm` 은 `domain` 에만 의존한다. 청크가 어디서 왔는지 알지 못한다.

- `domain` 은 프로젝트 내부의 어떤 모듈도 import 하지 않는다
- 각 계층은 자기보다 왼쪽만 import 한다. 순환 금지
- 이 규칙은 `tests/guards/test_layering.py` 가 AST 로 강제한다. 새 계층을 만들면
  `ALLOWED_IMPORTS` 에 등록해야 하며, 등록하지 않으면 테스트가 실패한다
- 도메인 모델이 계약이다. 상위 계층은 소스가 무엇인지 몰라야 한다
- 필터를 함수 시그니처 밖에 두지 않는다. 상위에서 한 번 걸러 넘긴다

### 아직 만들지 않은 것

`retrieval/`(P4–5), `evidence/`(P6), `agents/`(P8), `eval/`(P3)는 해당 Phase 에
도달했을 때 만든다. 빈 디렉터리를 미리 만들지 않는다.

---

## 현재 상태

**Phase 3 진행 중 — 평가셋과 기준선.** 사람 채점과 judge 일치율 측정이 남았다.

| Phase | 내용 | 상태 |
|---|---|---|
| 0 | 스캐폴드, 품질 게이트, ADR | ✅ |
| 1 | 문서 수집·파싱·청킹 (LLM 없음) | ✅ |
| 2 | 첫 LLM 호출, 구조화 출력 | ✅ |
| 3 | 평가셋 — 골든 질문 30개 | 진행 중 |
| 4 | 임베딩, 벡터 DB, 청킹 전략 실험 | |
| 5 | 하이브리드 검색 + 리랭킹, recall@k | |
| 6 | 근거 추적 | |
| 7 | 컨텍스트 예산, 프롬프트 캐싱 | |
| 8 | LangGraph 에이전트 루프 | |
| 9 | 관측, 모델 라우팅, 비용 | |
| 10 | Docker, 배포, CI | |

### Phase 진행 규약

**시작 전 선행 확인** — 이 Phase 가 전제하는 것이 실제로 존재하는가, 명세가 실물과
일치하는가, 이전 Phase 의 미결이 있는가. **코드를 쓰기 전에 확인하고 멈춰서 보고한다.**

**끝나면 멈춘다** — 승인 없이 다음 Phase 로 넘어가지 않는다. 보고에 포함할 것:
만든 것 / 명세와 다르게 한 것과 이유 / 발견한 문제 / 사용자가 결정할 것 /
검증 결과(테스트 수, skip 수, 게이트 통과 여부).

---

## 확정된 사실 (실측)

- 대상: **PostgreSQL 18**, 인덱싱 기준 **18.6**. 19 는 Beta 3 진행 중
- 수집: `https://ftp.postgresql.org/pub/source/v18.6/postgresql-18.6-docs.tar.gz`
  (3.9 MB, 1453 entries, `postgresql-18.6/doc/src/sgml/html/*.html`)
  — 공식 docs 페이지에는 PDF 만 링크되어 있고 HTML tarball 은 여기에만 있다
- tarball HTML 은 웹사이트 HTML 과 본문이 동일하고 사이트 크롬만 없다
  (errcodes 페이지 기준 `<tr>` 310, `<td>` 577 동일 / `<script>` 0 vs 2)
- XHTML 1.0 Transitional 이라 well-formed XML 로 엄격 파싱 가능
- 문서에 `id="ERRCODES-APPENDIX"` 같은 의미 있는 앵커가 있어 절 단위 근거 링크가 가능
- 섹션 컨테이너 **4,023개 전부가 id 를 가진다(100%)**
- 설정 파라미터는 표가 아니라 `<dl class="variablelist">` 다. `<dt>` 12,992개 vs 표 486개
- 문서 1,148개 전부가 표준 라이브러리 `ElementTree` 로 파싱된다 — lxml 이 필요 없다
- 파이프라인 산출: 문서 1,131 → 청크 16,442 (제외 7), 1.7초
- `chunk_id` 는 재실행 시 100% 동일하고, **18.4→18.6 두 릴리스를 건너뛰어도 100% 유지**된다.
  다만 58개(0.35%)는 id 가 같은데 본문이 바뀌었다 — 평가셋이 정답 청크 해시를 함께 저장하는 이유
- 무료 티어 실측 한도: Flash-Lite 분당 15 / 하루 500, Flash 계열 분당 5 / 하루 20.
  **SDK 재시도는 레이트 리밋에 부족하다**(백오프 15초 vs 윈도우 60초) — 호출 간격 4초로 막는다
- **temperature 를 지정하지 않으면 공급사 기본값(비결정적)이다.** 같은 20문항을 같은
  모델·프롬프트로 두 번 돌렸더니 통과율이 90% 와 75% 로 갈렸다. 0 으로 고정한 뒤에는
  101개 항목이 **전부 재현**된다. 평가 재현성의 전제다
- **캐시에는 LLM 응답만 담는다.** 채점 결과까지 담으면 채점기를 고쳤을 때 낡은 판정이
  재사용된다. 채점은 공짜이므로 캐시할 이유가 없다
- **첫 기준선은 75%다(improve 20문항, temperature 0).** 그 전에 나온 90% 는 비결정적
  실행의 한 표본이며 기준선이 아니다. 떨어진 것이 아니라 처음 제대로 잰 것이다
- **실패의 82%가 형식 문제다**(인용문은 코퍼스에 실재, 내용은 맞음). 형식을 전부 고치면
  89%→98% 가 되지만 답변이 좋아진 부분은 0 이다. §10 에서 형식/내용을 나눠 세지 않으면
  "인용 형식 준수율" 을 "답변 품질" 로 착각한다(ADR 0007 §8)
- **채점 항목이 서로 독립적이지 않다.** 근거 하나가 탈락하면 `cited_ids_exist` ·
  `quotes_verified` · `gold_cited` 가 동시에 무너져 한 원인이 3배로 세어진다.
  문항 단위 `pass_rate` 와 항목별 집계를 **함께** 봐야 한다(ADR 0007 §9)
- LLM: Gemini 무료 티어 Flash 계열. `gemini-2.5-flash` 는 신규 사용자에게 404 이고,
  `gemini-3.8-flash` 는 503 이 잦다. 기본값 `gemini-3.7-flash`(ADR 0004)
- google-genai SDK 기본 재시도: 5회 / 1s→60s 지수 백오프 / 408·429·500·502·503·504.
  **429 가 포함되어 있어 무료 티어 분당 제한은 SDK 가 처리한다**(ADR 0006)
- 컨텍스트에 `[chunk_id]` 대괄호 표기를 쓰면 모델이 대괄호까지 복사해 인용한다.
  `<chunk id="...">` 로 바꿔 원인을 없앴고, 정규화는 안전망으로만 둔다
- 청크의 참조 메타(`ref`)가 본문 대비 약 89%다. **프롬프트 비용이 아니라 저장 비용**이다
  — 프롬프트에는 `<chunk id="...">본문</chunk>` 만 들어가고 `ref` 는 JSONL 에만 있다

## 미결 항목

- **질문에 버전이 명시되지 않았을 때의 동작** — 최신 가정 / 사용자에게 질문 /
  여러 버전 병기. Phase 1 종료 시 실측과 함께 결정한다. 실물을 보기 전에 정하지 않는다
- **버전 메타데이터 스키마** — major(`18`)와 full(`18.6`)을 분리한다. 질문은 major 로
  들어오고 재현성은 full 로 걸린다. Phase 1 에서 확정
- **`.env.example`** — Phase 2 에서 LLM 설정이 생길 때 만든다
- **`MIN_QUOTE_CHARS = 20`** — 측정 없이 정한 값이다. Phase 3 에서 인용 길이 분포와
  탈락률을 보고 조정한다(ADR 0005)
- **프롬프트 방식** — zero-shot 고정. few-shot / CoT 비교는 Phase 3 이후(ADR 0004)
- **폴백과 평가의 충돌** — 평가 중 모델 폴백이 일어나면 결과를 그대로 비교하면 안 된다.
  Phase 3 평가 실행은 단일 모델 고정을 검토한다(ADR 0006)
- **기준선 실패를 세 갈래로 나눠 센다** — §10 에서 섞으면 무엇이 무엇을 고쳤는지 알 수 없다
  - **(A) `chunk_id` 축약** — `e04`, `k08`. 모델이 `runtime-config-LOCKS:0` 이라고 썼다.
    정답은 `runtime-config-locks#RUNTIME-CONFIG-LOCKS:0` 이고 인용문 자체는 정확했다.
    **id 표기 또는 프롬프트 문제**다 — Phase 2 의 대괄호 문제와 같은 계열
  - **(B) 긴 인용의 변형** — `k01`(618자 인용 중 뒤 265자만 일치), `c07`(`[...]` 생략 삽입),
    `k05`(표 앞 `| ` 누락). 여러 문장을 이어 쓰면서 미세하게 바뀐다.
    **인용 길이를 제한하는 프롬프트**로 고쳐질 여지가 있다
  - **(C) 정답 일부 누락** — `e02`(1/2), `e08`(2/3). `all_gold_cited` 가 잡는다.
    검색이 아니라 생성 단계의 문제다 — 정답 청크를 이미 주입했는데 일부만 인용했다
- **`no_missing_exception` 항목이 애매할 수 있다** — "중요한" 의 기준이 사람마다 다르다.
  일치율이 낮으면 항목을 둘로 쪼갠다(ADR 0008). 측정 전에 고치지 않는다
- **짧은 청크** — 50자 미만이 13.95%(2,294개)다. `<pre>` 사이에 낀 `or` 같은 연결어가
  독립 청크가 된다. 검색 노이즈가 될 가능성이 높지만 **평가셋 없이 고치지 않는다**.
  Phase 3 기준선 확보 후 측정과 함께 결정한다(ADR 0003)

## 문서

- `docs/decisions/` — ADR. 결정을 바꾸려면 **새 ADR 을 먼저 쓴다**.
  실측 없이 쓴 ADR 은 가설이다. "실측"이라고 적을 때는 실제로 측정했을 때만
- `log.md` — 주간 회고. 6~7개월 뒤 발표 자료의 원천
